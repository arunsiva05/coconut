# Core Airflow
apache-airflow==3.1.6

# Databricks provider — sensors and operators
# Requires: DatabricksSqlSensor, DatabricksRunNowOperator
apache-airflow-providers-databricks>=6.0.0

# Azure provider — WasbHook (BlobServiceClient) for ADLS Gen2 operations
# Requires: WasbHook with Managed Identity support
apache-airflow-providers-microsoft-azure>=10.0.0

# YAML config parsing
PyYAML>=6.0.1
